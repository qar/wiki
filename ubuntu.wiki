1. restarting networking: `sudo /etc/init.d/networking restart`
2. start on boot: `sudo update-rc.d <service-name> disable|enable`
3. install ssh: `sudo apt-get install openssh-server openssh-client`
